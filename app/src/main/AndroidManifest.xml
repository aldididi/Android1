<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pnj.uts.aldy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TMJReg"
        android:usesCleartextTraffic="true">
        <activity android:name="pnj.uts.aldy.HomeActivity"></activity>
        <activity android:name="pnj.uts.aldy.DetailBeritaActivity" />
        <activity android:name="pnj.uts.aldy.LatihanListActivity" />
        <activity android:name="pnj.uts.aldy.LoginActivity" />
        <activity android:name="pnj.uts.aldy.MainActivity"
            android:theme="@style/Theme.TMJRegNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>